# 🔐 SIEM Log Analyzer

> **Skills demonstrated:** SIEM platforms (Splunk/Sentinel logic) · Threat detection · Incident Response · MITRE ATT&CK mapping · NIST 800-53 / ISO 27001 / ASD Essential Eight compliance

A production-grade, multi-format Security Information and Event Management (SIEM) log analysis engine built in pure Python. Designed to mirror the core logic used in enterprise tools like **Splunk**, **Microsoft Sentinel**, and **IBM QRadar** — the skills most sought after in Australian and UK visa-sponsored cybersecurity roles.

---

## 📋 Why This Project

Analysing job listings on SEEK, Indeed AU, and UK Visa Jobs reveals the top required skills for sponsored cybersecurity roles are:

| Skill | Where Used in This Project |
|---|---|
| SIEM platforms (Splunk / Sentinel) | Core correlation engine mirrors Splunk SPL logic |
| Threat detection & analysis | 5 detection rules with sliding-window correlation |
| Incident Response | Alert triage scoring, recommended actions |
| MITRE ATT&CK knowledge | Every alert maps to ATT&CK technique IDs |
| Compliance (NIST, ISO, Essential Eight) | All alerts include control references |
| Python scripting | Entire codebase |
| Log parsing (Syslog, Apache, CloudTrail) | Multi-format parser with regex + JSON |

---

## 🏗 Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     SIEMPipeline                            │
│                                                             │
│  ┌──────────────┐   ┌──────────────────┐   ┌────────────┐  │
│  │  LogParser   │──▶│CorrelationEngine │──▶│  Reporter  │  │
│  │              │   │                  │   │            │  │
│  │ • Syslog     │   │ • 5 Rules        │   │ • JSON     │  │
│  │ • Apache     │   │ • Sliding Window │   │ • Markdown │  │
│  │ • CloudTrail │   │ • MITRE Mapping  │   │            │  │
│  └──────────────┘   └──────────────────┘   └────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 Quick Start

### Prerequisites
- Python 3.11+
- No external dependencies (stdlib only)

```bash
git clone https://github.com/F45elix/siem-log-analyzer.git
cd siem-log-analyzer

# Run full demo (generates synthetic logs + full report)
python main.py --demo

# Analyse your own log file
python main.py --log /var/log/auth.log --format syslog --report reports/

# Run tests
pip install pytest
pytest tests/ -v
```

---

## 🔍 Detection Rules

| Rule ID | Name | Severity | MITRE Technique |
|---|---|---|---|
| RULE-001 | SSH Brute Force | HIGH | T1110.001 |
| RULE-002 | Horizontal Port Scan | MEDIUM | T1046 |
| RULE-003 | Privilege Escalation (off-hours sudo) | CRITICAL | T1548.003 |
| RULE-004 | Data Exfiltration Beacon (>50MB) | CRITICAL | T1041 |
| RULE-005 | Impossible Travel / GeoIP Anomaly | HIGH | T1078 |

---

## 📊 Sample Report Output

```
## 🔴 [CRITICAL] Privilege Escalation via sudo
**Alert ID:** `a3f1b2c4d5e6f7a8`
**Risk Score:** 95/100
**MITRE ATT&CK:** T1548.003 - Abuse Elevation Control Mechanism
**Compliance Refs:** NIST AC-6, ISO A.9.2.3, Essential Eight Admin Privileges

> **Recommended Action:** Immediately revoke session; alert security team; preserve memory forensics.
```

---

## 📁 Project Structure

```
siem-log-analyzer/
├── src/
│   └── analyzer.py      # Core engine (parser, correlator, reporter)
├── tests/
│   └── test_analyzer.py # Full unit + integration test suite
├── sample_logs/         # Auto-generated by --demo flag
├── reports/             # Output directory for JSON + Markdown reports
├── main.py              # CLI entry point
├── requirements.txt
└── README.md
```

---

## 🎓 Relevant Certifications

This project is aligned with skills tested in:
- **CompTIA Security+** (threat detection, incident response)
- **GIAC GCIA** (log analysis, network forensics)
- **Splunk Core Certified User**
- **Microsoft SC-200** (Sentinel/SIEM operations)
